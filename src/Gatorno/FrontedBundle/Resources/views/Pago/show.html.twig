{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pago</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Fechadepago</th>
                <td>{{ entity.fechaDePago|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Pagado</th>
                <td>{{ entity.pagado }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('pagosClientes') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('pagosClientes_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
