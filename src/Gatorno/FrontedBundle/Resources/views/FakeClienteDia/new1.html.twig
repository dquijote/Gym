{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FakeClienteDia creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('count_dia') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
