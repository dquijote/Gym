{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FakeClienteDia list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Clientediamax</th>
                <th>Clientediamin</th>
                <th>Porciento</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('count_dia_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.ClienteDiaMax }}</td>
                <td>{{ entity.ClienteDiaMin }}</td>
                <td>{{ entity.porciento }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('count_dia_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('count_dia_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('count_dia_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
