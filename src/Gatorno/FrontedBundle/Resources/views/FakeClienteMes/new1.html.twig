{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FakeClienteMes creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('count') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
